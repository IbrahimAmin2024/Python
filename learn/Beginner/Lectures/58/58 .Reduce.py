# reduce() = apply a function to an iterable and reduce it to single value
#performs function on first two elements and repeats process until 1 value remains
# reduce(function, iterable)

import functools

# letters = ["H","E","L","L","O"] #HELLO
# reduce_function = lambda elem_1,elem_2: elem_1 + elem_2
# THe_word = functools.reduce(reduce_function,letters)
# print(THe_word)

# Numbers = [1,2,3,4,5,6,7,8,9] #45
# reduce_function = lambda elem_1,elem_2: elem_1 + elem_2
# The_numer = functools.reduce(reduce_function,Numbers)
# print(The_numer)